{
    "$schema": "https://json-schema.org/draft-07/schema#",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "runtimePath": {
            "type": "string",
            "description": "Path to a runtime executable, relative to the folder the '.noa' folder is in. If specified, overrides any default runtime path.",
            "default": "path/to/noa_runtime"
        }
    }
}
