func createCounter() {
    let mut x = 0;

    {
        add: (amount) => {
            x += amount;
        },
        get: () => x
    }
}

let counter = createCounter();

counter.add(2);
counter.add(3);
counter.add(4);

print(counter.get());
